module Elm19Compatible.Regex exposing (contains, fromString, never, replace)

import Regex as Regex19


never =
    Regex19.never


fromString =
    Regex19.fromString


contains =
    Regex19.contains


replace =
    Regex19.replace
